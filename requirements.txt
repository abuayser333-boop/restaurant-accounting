# requirements.txt
# نظام محاسبة المطاعم - المكتبات المطلوبة
# Python 3.8+

# قاعدة البيانات - مدمجة مع Python (لا تحتاج تثبيت)
# sqlite3

# لا توجد مكتبات خارجية مطلوبة للنسخة الأساسية
# البرنامج يعمل بمكتبات Python القياسية فقط:
# - sqlite3    (قاعدة البيانات)
# - datetime   (التواريخ)
# - json       (تخزين البيانات)
# - dataclasses (نماذج البيانات)
# - os, sys    (النظام)

# ===================================
# مكتبات اختيارية للتطوير المستقبلي:
# ===================================

# لإنشاء تقارير PDF:
# reportlab==4.0.7

# لتصدير إلى Excel:
# openpyxl==3.1.2

# لواجهة رسومية:
# tkinter  # مدمج مع Python

# لواجهة ويب:
# flask==3.0.0
